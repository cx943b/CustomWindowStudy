<Window x:Class="CustomWindowStudy.Controls.WindowGlowBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomWindowStudy.Controls"
             mc:Ignorable="d" Margin="0"
             d:DesignHeight="200" d:DesignWidth="20"
        AllowsTransparency="True"
        Background="{x:Null}"
        WindowStyle="None">
    <Grid Background="Crimson">
        <!--<Border x:Name="Glow" Background="Red" Width="1" HorizontalAlignment="Right" VerticalAlignment="Stretch">
            <Border.Effect>
                <DropShadowEffect Color="Red" BlurRadius="10" ShadowDepth="0" Opacity="1" />
            </Border.Effect>
        </Border>-->
    </Grid>
</Window>
